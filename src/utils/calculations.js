export const calculatePVC = (data) => {
  // PVC 계산 로직
  return data.pvc || 0;
};

export const calculateBV = (data) => {
  // BV 계산 로직
  return data.bv || 0;
};

export const calculateSV = (data) => {
  // SV 계산 로직
  return data.sv || 0;
}; 